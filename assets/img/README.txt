Drop the profile photo here, named exactly:

    portrait.jpg

It sits on a dark background — ideal. The CSS applies a warm
duotone treatment (grayscale → sepia → hue-shift) plus a vignette,
so any dark-background portrait will blend into the chart.
Recommended: ~800x1000px (4:5), JPG under 300KB for fast loads.

Until you add it, the hero shows a styled "PORTRAIT TILE MISSING"
placeholder instead of a broken image.
